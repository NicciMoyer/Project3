import React from "react";

function Roster () {

    return (
        <div>
        <h3>users for ${this.class}</h3>
        <br></br>
        <ul>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
            <li>${user.name}</li>
           
          </ul>
          </div>

    )
};

export default Roster;