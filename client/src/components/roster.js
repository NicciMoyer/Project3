import React from "react";

function Roster () {

    return (
        <div>
        <h3>Students for ${this.class}</h3>
        <br></br>
        <ul>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
            <li>${student.name}</li>
           
          </ul>
          </div>

    )
};

export default Roster;